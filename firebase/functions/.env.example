# URL of the Python AI HTTP Service (api.py)
AI_SERVICE_URL=http://127.0.0.1:8000

# Request timeout in milliseconds when calling the Python AI Service
AI_SERVICE_TIMEOUT_MS=30000

# Optional CORS allowed origin for HTTP endpoints
CORS_ORIGIN=*
